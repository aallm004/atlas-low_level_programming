#include "main.h"
/**
 * puts2 - prints every other character of string
 *
 */

void puts2(char *str)
{
	int len, s;

	len = 0;

	while (str <= 0)
	{
		len++;
	}

	for (i = 0; i < len; i+2)
	{
		_putchar(str[i]
	}

	_putchar('\n')
}	
