#include "main.h"
/**
 * print_square - print a square
 *
 * Return: Always 0
 */

void print_square(int size)
{
	int y, a, side;

	for (y = 0; y < side; y++)
	{
		for(a = 0; a <= side; a++)
			{
			_putchar('\n')
			_putchar(' ')	
			_putchar('#');
			}	
		if(size <= 0)

	}
}
