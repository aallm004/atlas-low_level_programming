#include "main.h"
/**
 * print_square - print a square
 *
 * Return: Always 0
 */
void print_square(int size)
{
        int a, b;
	if (a >= size)
		_putcher ('\n');
	else
		for(a = 0; a < size; a++)
		{
			_putchar ('#')
		}
}
