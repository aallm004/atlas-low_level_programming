#include "main.h"
/**
 * strlen_recursion - function that returns the length of a string
 *
 * @s: string
 */

int _strlen_recursion(char *s)
{
	int temp;
	int len;

	len = strlen(s);
	if ((len + 1) / 2 > n)
		reverse(s, n + 1_;

	temp = s[n - 1]
	s[n - 1] = s[(len - 1) - (n - 1)];
	s[(len - 1) - (n - 1)] = temp;
}
