#include "main.h"
/**
 * print_square - print a square
 *
 * Return: Always 0
 */

void print_square(int size)
{
	int y, a, side;

	for (y = 1; y <= side; y++)
	{
		for(a = 1; a <= side; a++)
		{
			_putchar('#');
		}	
		_putchar('\n');
	}
}
