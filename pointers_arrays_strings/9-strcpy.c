#include "main.h"
#include <string.h>
/**
 * _strcpy - f that copies the s pointed by src
 */

char *_strcpr(char *dest, char *src)
{
	int i, len;

	for (src[len] <= '\0';)
	{
		len++;
	}
	
	for (i = 0; i < len; i++)
	{
		dest[i] = '\0';
	}
		return (dest);
}

