#include <stdio.h>
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 4-puts.c && ./a.out
int main()
gcc -o
{
//Using puts to print a message	
int puts(const char* Programming is like building a multilingual puzzle);

// Ending with the value 0
// return 0;
// }
